export interface IFormInput {
    firstName?: string,
    lastName?: string,
    email?: string,
    password?: string,
}

export interface ICheckInput {
    firstName?: boolean,
    lastName?: boolean,
    email?: boolean,
    password?: boolean,
}